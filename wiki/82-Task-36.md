1. In **survey.html**, add opening and closing `<strong></strong>` tags around “Welcome.” Notice that the element does not move. That’s because `<strong>` elements are inline, so they can share lines with other elements.

